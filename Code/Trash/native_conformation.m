function [native] = native_conformation(composition,conformation);
% Invalid function. No used.
% composition = string {H,P}
% conformation = string {l,s,r}
% -----------------------------
% native = {l,s,r}


if (~valid_configuration(conformation))
    error('invalid initial conformation');
end

% create valid_conformation(conformation)






end
